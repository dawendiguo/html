/*if($){
    $(document).ready{
        function(){
            $('p').addClass('tmp');
            $('p').text('jQuery successfully loaded and running!');
        }
    };
}*/
window.onload = function(){
    alert("just for look ");
        $('p').addClass("temp");
        $('p').text('jquery successfully loaded and running!');
    }
